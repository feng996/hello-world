符号链接和硬链接：
	符号链接：
		ln -s
		创建一个具体的文件，指向被链接的文件，删除被链接的文件会影响链接文件
		能创建目录的符号链接
	硬链接：
		ln 
		两个文件inode相同，是一个文件，，删除一个文件，另一个还有效。



变量设置方式 {
	${变量#关键字}
	${变量##关键字}

	${变量%关键字}
	${变量%%关键字}

	${变量/旧字符串/新字符串}
	${变量//旧字符串/新字符串}
}